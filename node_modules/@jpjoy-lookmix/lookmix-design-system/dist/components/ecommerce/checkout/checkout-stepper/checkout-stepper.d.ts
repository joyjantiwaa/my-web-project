export type Step = {
    id: number;
    label: string;
};
export type CheckoutStepperProps = {
    steps: Step[];
    currentStep: number;
};
export declare function CheckoutStepper({ steps, currentStep }: CheckoutStepperProps): import("react/jsx-runtime").JSX.Element;
