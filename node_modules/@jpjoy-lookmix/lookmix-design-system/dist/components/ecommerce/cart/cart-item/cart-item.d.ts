export type CartItemProps = {
    id: string;
    title: string;
    image: string;
    price: number;
    properties?: {
        label: string;
        value: string;
    }[];
    quantity: number;
    onQuantityChange: (id: string, quantity: number) => void;
    onRemove: (id: string) => void;
};
export declare function CartItem({ id, title, image, price, properties, quantity, onQuantityChange, onRemove, }: CartItemProps): import("react/jsx-runtime").JSX.Element;
