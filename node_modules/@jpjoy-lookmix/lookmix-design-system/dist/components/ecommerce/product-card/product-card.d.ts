export type ProductCardProps = {
    id: string;
    title: string;
    price: number;
    discountPrice?: number;
    imageUrl: string;
    brand?: string;
    tag?: string;
    isOutOfStock?: boolean;
    href: string;
    onClick?: () => void;
    onAddToCart?: (id: string) => void;
};
export declare function ProductCard({ id, title, price, discountPrice, imageUrl, brand, tag, isOutOfStock, href, onClick, onAddToCart }: ProductCardProps): import("react/jsx-runtime").JSX.Element;
