import { default as React, ReactNode } from 'react';
import { HeaderNavItem } from './header-nav-item-type';
export type HeaderProps = {
    variant?: 'website' | 'app';
    title?: string;
    navItems?: HeaderNavItem[];
    action?: ReactNode;
    onBack?: () => void;
    rightContent?: ReactNode;
    logoName?: string;
    logoHref?: string;
    customLogo?: ReactNode;
    className?: string;
    style?: React.CSSProperties;
};
export declare function Header({ variant, title, navItems, action, onBack, rightContent, logoName, logoHref, customLogo, className, style, }: HeaderProps): import("react/jsx-runtime").JSX.Element;
