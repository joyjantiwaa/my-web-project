import { default as React } from 'react';
import { TabBarItem } from './tab-bar-item-type';
export type TabBarProps = {
    items: TabBarItem[];
    selectedValue?: string;
    onTabChange?: (value: string) => void;
    fullWidth?: boolean;
    className?: string;
    style?: React.CSSProperties;
};
export declare function TabBar({ items, selectedValue, onTabChange, fullWidth, className, style, }: TabBarProps): import("react/jsx-runtime").JSX.Element;
