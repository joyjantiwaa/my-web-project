import { default as React } from 'react';
export type LabelProps = {
    children: React.ReactNode;
    /** ประเภทของ Label */
    variant?: 'default' | 'medium';
    /** สีของข้อความ */
    color?: 'primary' | 'secondary';
    /** คลาสเพิ่มเติม */
    className?: string;
    /** HTML Tag ที่ต้องการใช้ (default: span) */
    as?: 'span' | 'label' | 'p' | 'div';
};
export declare function Label({ children, variant, color, className, as: Component, }: LabelProps): import("react/jsx-runtime").JSX.Element;
