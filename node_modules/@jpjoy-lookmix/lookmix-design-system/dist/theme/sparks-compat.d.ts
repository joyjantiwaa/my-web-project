import { default as React, ReactNode } from 'react';
export declare function mergeTokenSchema(base: any, overrides: any): any;
export interface ThemeProviderProps {
    children?: ReactNode;
    overrides?: any;
    className?: string;
    style?: React.CSSProperties;
}
export declare function createTheme<T>(options: {
    tokens: () => T;
}): {
    ThemeProvider: ({ children, overrides, className, style }: ThemeProviderProps) => import("react/jsx-runtime").JSX.Element;
    useTheme: () => T;
};
