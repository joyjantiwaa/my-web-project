import { default as React } from 'react';
export interface IconProps {
    /** ชื่อไอคอนจาก Iconify เช่น "mingcute:home-line" */
    icon?: string;
    /** ขนาดความกว้างและสูง (px) */
    size?: number | string;
    /** คลาสสำหรับปรับแต่งเพิ่มเติม */
    className?: string;
    /** สีของไอคอน (ค่าเริ่มต้นคือ 'currentColor') */
    color?: string;
    /** สไตล์เพิ่มเติม */
    style?: React.CSSProperties;
    /** รองรับการนำ Component ไอคอนอื่นๆ มาใส่ข้างใน */
    children?: React.ReactNode;
}
export declare function Icon({ icon, size, className, color, style, children, ...rest }: IconProps): import("react/jsx-runtime").JSX.Element | null;
